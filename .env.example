# Copy to .env and fill in before `npm run prod:up`.

# The domain Caddy serves and requests a certificate for.
# Must already point at this server's public IP, with ports 80 + 443 reachable.
DOMAIN=example.com

# Let's Encrypt contact address (expiry notices).
ACME_EMAIL=you@example.com
