# Development: Astro dev server with hot reload on http://localhost:4321
services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "4321:4321"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      ASTRO_TELEMETRY_DISABLED: "1"
